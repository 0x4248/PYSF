Python Script Framework help

Commands:
    $ <CMD>             Run a command on your system e.g $ ping 1.1.1.1
    list                Lists all scripts
    search <TERM>       Searches trough scripts with a term
    use <SCRIPT>        Sets the script for PYSF to use
    info                Gets info about a script
    set <ARG> <DATA>    Sets a variable to argument to set data
    listargs            Lists all set args
    clearargs           Clears all set args
    run                 Runs a script with the set perimeters and arguments
    execute             Runs a script with the set perimeters and arguments
    create              Goes trough a setup to create a script in ./scripts/user
    exit                Closes PYSF
    quit                Closes PYSF
    e                   Closes PYSF
    q                   Closes PYSF
    help                Shows this message
    version             Shows the version